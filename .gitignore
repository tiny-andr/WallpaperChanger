# build outputs
bin/
obj/
dist_standalone/

# installer artifacts (keep the .iss script, drop generated binaries)
installer/*.exe
installer/*.exe.manifest

# runtime artifacts
*.log
WallpaperChanger.ini

# IDE
.vs/
.idea/
*.user

# temporary files: UI-driving harnesses, release notes, push logs
.tmp-*/
.tmp-*

# design handoff (prototype + assets from the design tool, not shipped)
design/

